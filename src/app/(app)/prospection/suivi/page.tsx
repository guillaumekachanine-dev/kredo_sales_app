import { SuiviSection } from "@/components/prospection/suivi"

// Onglet Suivi — cockpit d'action de la prospection (campagnes, échéances,
// interactions, reco IA, roadmap phase-4 synchronisée). L'analyse décisionnelle
// reste sur la Synthèse.
export default function ProspectionSuiviPage() {
  return <SuiviSection />
}
