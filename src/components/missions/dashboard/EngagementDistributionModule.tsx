"use client"
import { useState } from "react"
import { cn } from "@/lib/utils"
import { EngagementConstellation } from "./EngagementConstellation"
import { RevenueBreakdownChart } from "./RevenueBreakdownChart"
import type { EngagementsPortfolioViewModel } from "./engagements-portfolio-types"
export function EngagementDistributionModule({ overview }: { overview: EngagementsPortfolioViewModel }) { const [mode,setMode]=useState<"constellation"|"breakdown">("constellation"); return <section className="col-span-5 flex min-h-0 flex-col overflow-hidden rounded-[var(--radius-medium)] border border-border bg-surface p-4"><div className="mb-1 flex items-start justify-between gap-3"><div><h2 className="font-heading text-sm font-bold text-heading">{mode === "constellation" ? "Constellation des engagements" : "Répartition du CA"}</h2><p className="text-[10px] text-muted">{mode === "constellation" ? "Temps restant · marge réelle · CA réalisé" : "Même périmètre que le CA réalisé"}</p></div><div className="inline-flex rounded-[var(--radius-medium)] border border-border bg-canvas p-0.5">{(["constellation","breakdown"] as const).map((value)=><button key={value} type="button" aria-pressed={mode===value} onClick={()=>setMode(value)} className={cn("min-h-8 rounded-[var(--radius-small)] px-2 text-[9px] font-bold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40",mode===value?"bg-surface text-primary":"text-muted")}>{value==="constellation"?"Constellation":"Répartition"}</button>)}</div></div>{mode === "constellation" ? <EngagementConstellation points={overview.portfolio.points}/> : <RevenueBreakdownChart byPractice={overview.revenue.byPractice} byClient={overview.revenue.byClient} embedded/>}</section> }
